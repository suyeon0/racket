package com.racket.cash.consume.const

class DeadLetterType {

    companion object {
        const val INSERT_DB = "INSERT_DB"
        const val RETRY = "RETRY"
        const val REQUEST_ADMIN = "REQUEST_ADMIN"
    }

}